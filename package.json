{
  "name": "azure-get-keyvault",
  "version": "1.0.4",
  "description": "add env vars",
  "main": "src/main.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node main.js",
    "start:prod": "node dist/index.js",
    "build": "rm -rf ./dist && ncc build ./src/main.js -o dist"
  },
  "type": "module",
  "author": "Braydon Harris",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1",
    "@azure/identity": "^4.13.1",
    "@azure/keyvault-secrets": "^4.10.0"
  },
  "devDependencies": {
    "@types/node": "^22.18.10",
    "@vercel/ncc": "^0.38.4"
  }
}
