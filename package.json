{
  "name": "azure-get-keyvault",
  "version": "1.0.0",
  "description": "add env vars",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node main.js",
    "start:prod": "node dist/index.js",
    "build": "rm -rf ./dist && ncc build main.js -o dist"
  },
  "type": "module",
  "author": "Braydon Harris",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "@azure/identity": "^4.5.0",
    "@azure/keyvault-secrets": "^4.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.4.5",
    "@vercel/ncc": "^0.38.3"
  }
}
